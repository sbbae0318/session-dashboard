[project]
name = "session-summary-service"
version = "0.2.0"
description = "DSPy-powered incremental session summary (CLI)"
requires-python = ">=3.11"
dependencies = [
  "dspy>=2.0",
  "anthropic>=0.40",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.ruff]
line-length = 100
